# Copy to docker/.env and fill in your deployment's values.
# docker/.env is gitignored so real hostnames/IPs stay out of version control.

# Public hostname this deployment is reached at. Must publicly resolve to the
# server's IP. Used for the frontend's backend URL and Caddy's TLS certificate.
RESHIMIT_PUBLIC_HOST=your-host.example.com
